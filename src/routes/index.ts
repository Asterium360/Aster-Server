import { Router } from 'express';
//import auth from './auth.routes.js';
//import posts from './posts.routes.js';
const r = Router();
//r.use('/auth', auth);
//r.use('/posts', posts);
export default r;
